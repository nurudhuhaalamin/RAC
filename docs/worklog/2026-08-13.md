# Log Pekerjaan — 13 Agustus 2026

### [00:00] Catatan batas hari

Sesi ini dimulai 12 Agustus sore dan berjalan melewati tengah malam. **Entri rinci untuk
T-105, T-106, dan T-201 ada di `2026-08-12.md`**, meskipun pekerjaannya secara jam terjadi
pada 13 Agustus (T-105 selesai 05:53, T-106 06:07, T-201 06:17 WIB).

Entri tidak dipindahkan — ketiganya sudah ter-commit bersama kodenya, dan memindahkannya
justru memutus kaitan antara log dan commit-nya.

**Pelajaran untuk skill `worklog`:** sesi panjang yang melewati tengah malam memecah log ke
dua file, dan hook `check-worklog` memakai tanggal hari ini — sehingga ia memblokir meski
log sudah ditulis dengan lengkap. Hook-nya benar; yang kurang adalah petunjuk apa yang
harus dilakukan saat batas hari terlewati. Ditambahkan ke SKILL.md.

---

### [06:20] Ringkasan sesi

**Tujuan:** Mengeksekusi Fase 1 dan memulai Fase 2 dari `docs/PLAYBOOK.md`.

**Yang selesai:** 6 dari 25 tugas — T-101, T-103, T-104, T-105, T-106 (Fase 1 lengkap),
dan T-201 (tugas pertama Fase 2).

**Verifikasi terakhir yang dijalankan:**

```
pnpm typecheck    -> 0 errors, 0 hints
pnpm lint         -> All matched files use Prettier code style
pnpm test         -> 152 lulus, 0 gagal (5 file test)
pnpm test:e2e     -> 14 lulus (7 skenario x chromium + mobile)
pnpm check:content-> LULUS, 49 file dipindai, 0 pelanggaran
pnpm build        -> Complete
pnpm lighthouse   -> performance 1.00 | a11y 1.00 | best-practices 0.96 | seo 0.60
```

**Keputusan teknis lintas-tugas:**
- **A-13 / ADR-008** — test kontras menemukan tiga token DESIGN-SYSTEM Bab 3 gagal WCAG AA.
  Palet tidak diubah; ditambah token turunan. Didokumentasikan di kedua dokumen.
- **D-15** (Inter untuk isi artikel) dan **D-17** (tanpa E-Learning) ditutup.
- E2E dijalankan terhadap `wrangler dev`, bukan `astro dev` — menguji runtime Workers
  sungguhan, dan `astro dev` di Astro 7 berjalan sebagai daemon terlepas.
- Workflow deploy/preview dijaga `ENABLE_CLOUDFLARE_DEPLOY` supaya `skipped`, bukan merah.
  Check yang selalu gagal mengajari orang mengabaikan check.

**Masalah / catatan:**
- **Skor Lighthouse `performance 1.00` belum bermakna** — belum ada foto, font self-hosted,
  atau konten sungguhan. Ukur ulang setelah T-217. Menyebutnya pencapaian sekarang menyesatkan.
- **SEO 0,60** disebabkan `noindex` yang dipasang sengaja di halaman placeholder; satu-satunya
  audit yang gagal `is-crawlable`. Perilaku benar, bukan cacat.
- **`deploy.yml` dan `preview.yml` belum pernah terbukti** terhadap akun Cloudflare.
- **`docs/FAKTA-BISNIS.md` masih 72 `TODO`.** D-13 memblokir T-206, D-19 menentukan sah
  tidaknya klaim "We Certify", D-20 menentukan seksi Sertifikasi tayang atau tidak.
- Validator konten menangkap komentar buatan sendiri **dua kali** (file seed T-103, header
  `schema.ts` T-201) — kalimat larangan tetap memuat frasa terlarang. Keduanya diperbaiki
  kontennya, bukan daftar putihnya.

**Berikutnya:** `/clear`, lalu **T-202** (sitemap, robots.txt, llms.txt). 18 tugas Fase 2
tersisa. Setiap tugas sudah di-commit atomik, jadi melanjutkan tidak kehilangan apa pun.

**Commit:** lihat `2026-08-12.md` per tugas — `1cb57e6`, `2ec67cf`, `4629c0b`, `21b85e8`,
`e7f1ee2`, `3888ae2`, `c949cb5`
